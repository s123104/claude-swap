# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local agent/editor state
.claude/
.cursor/
output.txt
.pytest_cache/

# Stray Node artifacts — this is a pure-Python project (no package.json); a tool
# occasionally runs npm/npx here and drops an empty lockfile. Keep it out of the tree.
node_modules/
package-lock.json
.coverage

# Stray tooling state — an orchestration tool drops its runtime state here.
# Not a project artifact; keep it out of the tree.
.omc/

# superpowers session planning artifacts (not part of the project)
docs/superpowers/
